# Prompts

This file contains various prompts I used in the project.

## Project structuring

> I’m structuring a React TS app with api/, hooks/, context/, components/, and pages/.Selection state will live in Context and persist via localStorage. Is this correct

> My search filters and sorts data on every keystroke. why and how to fix it?

> How would I modify my infinite scroll implementation if data came from a paginated API instead of a static dataset?

## State Management Decisions

> I chose Context API over Redux for managing selection state across routes since the state is small and localized.  
> Is there any scenario here where Redux would be justified? If so, why should i choose it?

## Download Handling

> Direct download via anchor tag didn’t work due to cross-origin restrictions.  
> What is the correct way to handle downloading images from external URLs?

> Currently downloads trigger individually.
> What would be a better UX approach (e.g., batching or zipping files)?
